import { setRenderPattern, PATTERNS } from '../patterns';
import { setColorScheme, COLOR_SCHEMES } from '../colors';
import { getTargetZoom, setZoom, setCameraPreset, onZoomChange } from './camera';
import { toggleAxes } from './sketch';

export { sketch } from './sketch';

export { 
  setRenderPattern, 
  setColorScheme, 
  toggleAxes, 
  setZoom, 
  setCameraPreset,
  getTargetZoom,
  onZoomChange
};

export { 
  PATTERNS, 
  setOpacity 
} from '../patterns';

export { 
  COLOR_SCHEMES 
} from '../colors';